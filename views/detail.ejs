<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QuickBite Recipes</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
        }
        #container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }
        img {
            display: block;
            margin: 0 auto;
            max-width: 100%;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        ul, ol {
            list-style-type: none;
            padding: 0;
            margin-left: 20px;
        }
        li {
            margin-bottom: 10px;
        }
        p {
            margin: 0;
        }
        b {
            color: #333;
        }
        .print-button {
            display: block;
            width: 150px;
            margin: 20px auto;
            padding: 10px;
            background-color: #4caf50;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            text-align: center;
        }
        @media print {
            .print-button {
                display: none !important;
            }
            .hide {
                display: none !important;
            }
        }
        .print-button:hover {
            background-color: #45a049;
        }
        .photos {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }
        .ddd {
            display: flex;
            justify-content: space-around;
            margin-bottom: 8rem;
            width: 13rem;
        }
    </style>
</head>
<body>
    <div id="container">
        <h1><%= recipe.title %></h1>
        <div class="photos">
            <% for (var i = 0; i < recipe.photo.length; i++) { %>
                <img src="<%= recipe.photo[i] %>" height="250px">
            <% } %>
        </div>

        <p><b>Ingredients</b></p>
        <ol>
            <% for (var i = 0; i < recipe.ingredients.length; i++) { %>
                <li><%= recipe.ingredients[i] %></li>
            <% } %>
        </ol>
        <p><b>Instructions</b></p>
        <ol>
            <% for (var i = 0; i < recipe.instructions.length; i++) { %>
                <li><%= recipe.instructions[i] %></li>
            <% } %>
        </ol>

        <div class="ddd">
            <form action="/recipe/<%= recipe._id %>?_method=DELETE" method="post">
                <button class="btn btn-primary hide">Delete</button>
            </form>
    
            <a href="/recipe/<%=recipe._id%>/edit" class="btn btn-warning hide">Edit</a>
        </div>

        

        <button class="print-button" onclick="printRecipe()">Print Recipe</button>
    </div>

    <script>
        function printRecipe() {
            window.print();
        }
    </script>
</body>
</html>
